server:
  port: 9000
spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

eureka:
  instance:
    hostname: localhost
 #Add the routes here     
#eureka:
#  client:
#    service-url:
#      defaultZone: http://127.0.0.1:8761/eureka
#
#  instance:
#    prefer-ip-address: true


            
         
      
